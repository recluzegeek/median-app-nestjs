import { PrismaExceptionFilterFilter } from './prisma-exception-filter.filter';

describe('PrismaExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new PrismaExceptionFilterFilter()).toBeDefined();
  });
});
